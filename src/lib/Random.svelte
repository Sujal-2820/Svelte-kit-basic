<h3>Accessibility Adjustments:

    Replaced <div> with <button> for interactive elements.
    Added focus outline for keyboard accessibility.
    JavaScript Adjustments:
    
    Kept the function to change the current artist and update the progress bar.
    CSS Adjustments:
    
    Styled the button to look like the previous div.
    Ensured the image container remains fixed and the transitions are applied smoothly.
    These adjustments should ensure the image container remains fixed, the images change with a fade-up effect, and the elements are accessible.
</h3>
<br/>
<h3>
C:/Users/HP/OneDrive/Desktop/The Graph Verse/svelte/basic/src/lib/Artists.svelte:54:6 A11y: visible, non-interactive elements with an on
event must be accompanied by a keyboard event handler. Consider whether an interactive element such as
</h3>